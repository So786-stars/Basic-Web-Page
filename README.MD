# By using this file i known some basic  commands : -<br>
# How the command are work properly and how i am excute . <br>
# In this Repository i am using basic css and HTML to understand the basic commands of git hub like : -<br>

// What i exectly want to do in this module.
// try to push the git file from local system to remote system. 
<!-- 
step 0 :- we create the all files or modules and commit it after that 
step 1 :- first creating a reposatry on Github.
step 2 :- git init (init is creating the new git repo . for conformation we use ( ls -a/ls -force) in msc/window os ) 
step 3 :- git remote origin ..repo_link... (add the origin of the file that we created on GitHub)
step 4 :- git remote -v (use this ccommand to verify the remote)
step 5 :- git branch (to check the branch name is it is master or main )
step 6 :- git branch -M main (to rename the existing branch name)
step 7 :- git push origin main (to push the code on Github Platform )

-->

// now our file is push on git hub we check it or it shown when we refresh it
